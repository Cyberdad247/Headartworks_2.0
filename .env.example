# Shopify Storefront API Configuration
# INSTRUCTIONS: Copy this file to .env and replace the placeholder values

# Your Shopify store domain (e.g., headartworks.myshopify.com)
PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# Storefront API access token - Get this from:
# Shopify Admin → Apps → Develop apps → Create app → Configure Storefront API
PUBLIC_STOREFRONT_API_TOKEN=your_storefront_api_token_here

# Session secret - Generate a secure random string (32+ characters)
# You can use: openssl rand -base64 32
SESSION_SECRET=your_secure_session_secret_here

# Checkout domain (usually same as store domain)
PUBLIC_CHECKOUT_DOMAIN=your-store.myshopify.com

# Storefront ID for analytics (optional)
PUBLIC_STOREFRONT_ID=gid://shopify/Storefront/your_storefront_id

# Development/Production environment
NODE_ENV=development

# Optional analytics
GA_TRACKING_ID=your_ga_tracking_id
